reshuffle_forecast <- function(pp_forecast, template){
  # pp_forecast: the post-processed forecast that has an unrealistic dependence structure
  # template: the dependence template that comes either from the raw forecast (ECC) or the observations (Schaake Shuffle)
}
